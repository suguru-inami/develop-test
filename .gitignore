# .gitignore for jet-llms-txt-generator

# OS generated files
.DS_Store
Thumbs.db

# IDE/Editor directories and files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Dependency directories (if using Composer or npm)
/vendor/
/node_modules/

# Build artifacts (if any)
/build/
/dist/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# WordPress specific (usually not needed inside plugin dir, but for safety)
# wp-config.php # Should never be in plugin repo
# wp-content/uploads/ # Should never be in plugin repo
# .htaccess # Sometimes generated

# Local development tool specific (example for Local)
# (Usually outside the plugin directory, but good practice if config files appear)